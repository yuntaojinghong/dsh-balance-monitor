# dsh-balance-monitor 组合补丁：向 web profile 插入本插件行
# 由 `dsh plugin --profile web add dsh-balance-monitor` 安装后自动作为 bundle 层组合
- insert:
    - id: balance-monitor
      name: dsh-balance-monitor
      config: {}
